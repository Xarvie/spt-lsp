{
  // 1. 注释规则
  "comments": {
    "lineComment": "//", // 定义行注释
    "blockComment": [ "/*", "*/" ] // 定义块注释的开始和结束标记
  },

  // 2. 括号定义 (用于匹配和高亮)
  "brackets": [
    ["{", "}"],
    ["[", "]"],
    ["(", ")"]
  ],

  // 3. 自动闭合配对 (核心功能)
  "autoClosingPairs": [
    // 括号自动闭合
    { "open": "{", "close": "}" },
    { "open": "[", "close": "]" },
    { "open": "(", "close": ")" },
    // 引号自动闭合，但不在字符串或注释内部触发
    { "open": "'", "close": "'", "notIn": ["string", "comment"] },
    { "open": "\"", "close": "\"", "notIn": ["string", "comment"] }
    // 如果你的语言支持其他配对，例如模板字符串 ``, 可以在这里添加
    // { "open": "`", "close": "`", "notIn": ["string", "comment"] }
  ],

  // 4. 按下 Tab 键时，移除的配对符号 (主要用于 Tab 跳出括号/引号)
  "autoCloseBefore": ";:.,=}])>` \n\t",

  // 5. 包围功能支持的配对 (用于 VS Code 的 "Surround With" 命令)
  "surroundingPairs": [
    ["{", "}"],
    ["[", "]"],
    ["(", ")"],
    ["'", "'"],
    ["\"", "\""]
    // ["`", "`"] // 如果支持模板字符串
  ],

  // 6. 代码折叠规则
  "folding": {
    // 使用标记来定义可折叠区域 (可选，但常用)
    "markers": {
      "start": "^\\s*//\\s*#region\\b", // 匹配以 // #region 开头的行
      "end": "^\\s*//\\s*#endregion\\b" // 匹配以 // #endregion 开头的行
    }
    // 注意：VS Code 通常会基于 "brackets" 和 "comments" 中的定义
    // 自动启用对 {} 和 /* */ 的折叠。
  },

  ////vscode能智能处理回车缩进
  // // 7. 回车时的自动缩进规则 (基本规则)
  // "onEnterRules": [
  //   {
  //     // 在以 '}', ']', ')' 结尾的行之前输入回车，下一行取消缩进
  //     "beforeText": "^\\s*[\\}\\]\\)]",
  //     "action": { "indent": "outdent" }
  //   },
  //   {
  //     // 在以 '{', '[', '(' 结尾的行之后输入回车，下一行缩进
  //     "beforeText": "^.*[(\\[\\{]\\s*$",
  //     "action": { "indent": "indent" }
  //   }
  //   // 可以根据语言特性添加更复杂的规则，例如在 if/for/while 等关键字后缩进
  //   // 但对于强制使用 {} 的语言，通常基于括号的规则就足够了。
  // ]

  // 8. Word Pattern (可选)
  // "wordPattern": "/(-?\\d*\\.\\d\\w*)|([^\\`\\~\\!\\#\\$\\%\\^\\&\\*\\(\\)\\=\\+\\[\\{\\]\\}\\|\\;\\:\\'\\\"\\,\\.\\<\\>\\/\\?\\s]+)/g"
  // 定义了哪些字符构成一个“单词”，影响双击选中、词语导航等。
  // 通常 VS Code 的默认设置已经够用，除非遇到特殊问题。
}